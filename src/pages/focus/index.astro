---
import Layout from '@/layouts/Layout.astro';
import { FocusApp } from '@/components/FocusApp';
import { baseUrl } from '@/lib/base-url';
---

<Layout pageKey="focus" hideCat>
  <FocusApp client:only="react" baseUrl={baseUrl()} />
</Layout>